describe.skip('Part 2 Change 8: human-friendly timestamps', () => {
  test('formats past timestamps as relative human-friendly text', () => {});
  test('formats near-future timestamps as relative human-friendly text', () => {});
  test('formats upcoming timestamps with locale-aware day and time labels', () => {});
  test('uses the viewer timezone when displaying human-friendly times', () => {});
});
