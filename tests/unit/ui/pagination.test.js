describe.skip('Requirement 12: pagination', () => {
  test('returns the first page of a larger result set', () => {});
  test('returns later pages without duplicating results from earlier pages', () => {});
  test('reports whether previous and next pages are available', () => {});
  test('preserves sort order when paginating history results', () => {});
});
