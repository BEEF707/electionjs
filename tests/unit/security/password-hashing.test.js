describe.skip('Requirement 13: password hashing', () => {
  test('stores password hashes instead of cleartext passwords', () => {});
  test('uses a unique salt when hashing user passwords', () => {});
  test('verifies a correct password against the stored hash', () => {});
  test('rejects an incorrect password against the stored hash', () => {});
});
