describe.skip('Requirements 1, 3, 4, and 5: role permissions', () => {
  test('allows voters to vote only in elections assigned to them', () => {});
  test('allows administrators to view elections but not cast votes', () => {});
  test('allows moderators to manage voters only for assigned elections', () => {});
  test('allows reporters to view only assigned closed election history and nothing else', () => {});
  test('hides deleted elections from non-administrators', () => {});
  test('allows administrators to see voter real names associated with votes', () => {});
  test('prevents non-administrators from seeing voter real names associated with votes', () => {});
});
