describe.skip('Requirement 7: password recovery', () => {
  test('creates a password recovery token for a known user email', () => {});
  test('does not reveal whether an email address belongs to an account', () => {});
  test('allows a valid recovery token to set a new password', () => {});
  test('rejects expired or already-used recovery tokens', () => {});
});
