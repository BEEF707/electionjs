describe.skip('Requirement 6: login lockout', () => {
  test('tracks failed login attempts for a user', () => {});
  test('shows how many login attempts remain', () => {});
  test('locks a user out for 1 hour after 3 failed login attempts', () => {});
  test('resets failed attempt tracking after a successful login', () => {});
});
