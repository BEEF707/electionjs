describe.skip('Requirement 6: password strength', () => {
  test('classifies passwords of 10 characters or fewer as weak', () => {});
  test('classifies passwords from 11 to 17 characters as moderate', () => {});
  test('classifies passwords longer than 17 characters as strong', () => {});
  test('rejects weak passwords when administrators create users', () => {});
});
