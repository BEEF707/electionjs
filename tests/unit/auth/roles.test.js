describe.skip('Requirement 1: user roles', () => {
  test('supports voter, moderator, administrator, and reporter roles', () => {});
  test('prevents a user from having more than one role at a time', () => {});
  test('requires every user to be authenticated before interacting with the system', () => {});
  test('ensures exactly one super administrator exists', () => {});
});
