describe.skip('Part 2 Change 5: self-service profile editing', () => {
  test('allows users to update their own name and metadata from the dashboard', () => {});
  test('allows users to update their own email when the new email is unique', () => {});
  test('allows users to change their own password when it meets password strength rules', () => {});
  test('prevents users from changing their own role or approval status', () => {});
});
