describe.skip('Part 2 Change 9: open registration', () => {
  test('allows unauthenticated users to submit a sign-up request', () => {});
  test('creates newly registered users in a pending approval state', () => {});
  test('prevents pending users from accessing the authenticated system before approval', () => {});
  test('allows administrators to approve pending registered users', () => {});
  test('continues to allow administrators to create users manually', () => {});
});
