describe.skip('Requirement 9: instant-runoff voting', () => {
  test('requires voters to rank every election option', () => {});
  test('declares a winner immediately when an option has more than 50% of first-choice votes', () => {});
  test('eliminates the option with the fewest first-choice votes when there is no majority', () => {});
  test('transfers ballots from eliminated options to the next available ranked option', () => {});
  test('calculates Pizza as the winner in the problem statement example', () => {});
  test('supports partial rankings for externally owned elections when rendering results', () => {});
  test('reports each option elimination round or current winning position', () => {});
  test('reports first-choice ballot counts for each option', () => {});
});
