describe.skip('Requirement 2: election validation', () => {
  test('requires a title and description', () => {});
  test('requires creation, opening, and closing timestamps represented as unix epoch milliseconds', () => {});
  test('requires at least two ranked options', () => {});
  test('requires ballots to map a voter id to a complete ranking', () => {});
  test('tracks whether an election is deleted', () => {});
});
