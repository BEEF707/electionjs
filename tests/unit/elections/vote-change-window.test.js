describe.skip('Part 2 Change 10: vote change window', () => {
  test('allows voters to change or remove a ballot within five minutes of submitting it', () => {});
  test('prevents voters from changing or removing a ballot more than five minutes after submitting it', () => {});
  test('prevents voters from changing or removing a ballot after the election closes even if five minutes have not passed', () => {});
  test('uses the earlier deadline between five minutes after submission and election close time', () => {});
});
