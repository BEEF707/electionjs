describe.skip('Requirements 2, 3, and 4: election status', () => {
  test('identifies upcoming elections from opening and closing timestamps', () => {});
  test('identifies open elections from opening and closing timestamps', () => {});
  test('identifies closed elections from opening and closing timestamps', () => {});
  test('prevents non-super administrators from modifying closed elections', () => {});
  test('allows the super administrator to reopen a closed election by changing the close timestamp', () => {});
});
