describe.skip('Part 2 Change 1: election audit log', () => {
  test('records administrator changes to election title, description, opensAt, closesAt, and deleted status', () => {});
  test('records newly cast ballots without exposing voter names to non-administrators', () => {});
  test('records changed ballots without exposing voter names to non-administrators', () => {});
  test('records deleted ballots without exposing voter names to non-administrators', () => {});
  test('only requires audit logs for elections owned by this team', () => {});
});
