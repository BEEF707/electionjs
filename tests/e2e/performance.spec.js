const { test } = require('@playwright/test');

test.describe('Requirement 11: performance', () => {
  test.skip('dashboard reaches a usable state within the project performance budget', async () => {});
  test.skip('history sorting and pagination respond within the project performance budget', async () => {});
});
