describe.skip('Requirements 6 and 7: authentication routes', () => {
  test('renders the login page for unauthenticated users', () => {});
  test('renders a sign-up page for unauthenticated users', () => {});
  test('authenticates a user with valid credentials', () => {});
  test('prevents pending registered users from accessing authenticated routes before administrator approval', () => {});
  test('rejects invalid credentials without exposing sensitive details', () => {});
  test('logs out authenticated users', () => {});
  test('starts the password recovery flow without sending real email', () => {});
});
