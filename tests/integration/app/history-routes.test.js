describe.skip('Requirement 5: history routes', () => {
  test('lists closed elections in descending closing-time order by default', () => {});
  test('paginates closed election results', () => {});
  test('includes unowned closed elections in history', () => {});
  test('hides deleted elections from non-administrators', () => {});
  test('blocks reporters from viewing election results they are not assigned to', () => {});
  test('allows reporters to view election results they are assigned to', () => {});
  test('shows voter names associated with votes only to administrators', () => {});
  test('shows descriptive IRV result details for closed elections', () => {});
  test('allows sorting by title, creation time, opening time, and closing time', () => {});
});
