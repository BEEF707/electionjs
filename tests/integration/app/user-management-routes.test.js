describe.skip('Requirements 1, 4, and 6: user management routes', () => {
  test('allows administrators to create voters, moderators, and reporters', () => {});
  test('allows only the super administrator to create administrators', () => {});
  test('allows administrators to approve pending open-registration accounts', () => {});
  test('allows administrators and moderators to assign reporters to elections', () => {});
  test('allows administrators to update non-administrator users', () => {});
  test('allows the super administrator to delete other administrators', () => {});
  test('prevents the super administrator from deleting themselves', () => {});
});
